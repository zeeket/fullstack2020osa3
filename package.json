{
  "name": "osa3fullstackopen2020",
  "version": "1.0.0",
  "description": "separate repo for part 3 tasks",
  "repository": {
    "type": "git",
    "url": "git@github.com:zeeket/fullstack2020osa3.git"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf build && cd frontend && npm install && npm run build --prod && mv build ../build"
  },
  "author": "zeeket",
  "license": "MIT",
  "devDependencies": {
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "express": "^4.17.1",
    "mongoose": "^5.11.17",
    "morgan": "^1.10.0"
  }
}
